<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rl"
    android:layout_width="match_parent"

    android:layout_height="match_parent"
    tools:context=".Plan"
    >


    <android.support.v7.widget.Toolbar
        android:id="@+id/ctbarxp"
        android:layout_width="match_parent"

        android:layout_height="50dp"
        android:background="@color/colorPrimary"
        android:titleTextColor="@android:color/white"
      ></android.support.v7.widget.Toolbar>
<RelativeLayout
    android:layout_width="match_parent"
    android:padding="10dp"
    android:layout_below="@+id/ctbarxp"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/infoxer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"


        android:layout_centerHorizontal="true"
        android:text="Please choose a payment plan. A weekly plan (Ksh 500) valid for 7 days or a monthly plan (Ksh 1000 valid for 30 days)."
        android:textAlignment="viewStart"
        android:textAppearance="@style/TextAppearance.AppCompat.Caption"
        android:textColor="@android:color/black"
        android:textSize="16sp" />

    <EditText
        android:id="@+id/infox"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/infoxer"
        android:hint="Enter your phone number"
        android:inputType="phone"
        android:padding="14dp"
        android:textAlignment="viewStart"
        android:textColor="@color/button_facebook_pressed"
        android:textSize="19sp" />

    <ListView
        android:id="@+id/lvxp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="14dp"

        android:layout_below="@+id/infox" />
</RelativeLayout>
</RelativeLayout>